<!DOCTYPE html>
<html lang="en">
  <head>
    <%-include("./partials/head") %>
  </head>
  <body>
    <%-include('./partials/nav') %>
    <h1>Home</h1>
    <div class="container mt-3"><% blogs.forEach(blog => {%>
      <div class="card" style="width: 18rem;">
  <img src="<%=blog.CoverImg %>" class="card-img-top" alt="CoverImg">
  <div class="card-body">
    <h5 class="card-title"><%= blog.title %></h5>
    
    <a href="#" class="btn btn-primary">Go somewhere</a>
  </div>
</div>
      
    <%}); %></div>
    <%-include("./partials/script")%>
  </body>
</html>
